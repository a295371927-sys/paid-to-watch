# 带薪看片 MVP 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出一个伪装成右下角广告弹窗的本地视频播放器 Electron 桌面应用——选本地文件夹播视频,一键 Boss Key 把视频秒变假广告。

**Architecture:** Electron 应用。纯逻辑(视频扫描、广告轮播、配置合并)抽到 `src/core/` 做 CommonJS 纯函数并用 vitest 单元测试(TDD);Electron 主进程(窗口/托盘/全局热键/IPC)和渲染进程(三段式 UI/假广告/视频/Boss Key)做集成,用手动验收验证。

**Tech Stack:** Electron 31、vitest 2(单测)、原生 HTML/CSS/JS、CommonJS 模块。

---

## 前置说明

- 所有命令默认在**项目根目录** `8 其他方向/带薪看片/` 下执行。
- 模块格式:`src/` 代码用 **CommonJS**(`module.exports` / `require`),因为 Electron 主进程要 `require` 它们;`package.json` **不设** `"type": "module"`。
- 测试文件用 **ESM import**(vitest 自带转译,无需 `type: module`),通过 default 导入 CJS 模块对象再解构。
- 视频格式限定 Chromium `<video>` 能播的:`.mp4 .webm .ogv .m4v`(mkv/avi 播不了,首版不收)。

## 文件结构

```
8 其他方向/带薪看片/
├── package.json              # 依赖与脚本
├── vitest.config.js          # 测试配置(node 环境)
├── .gitignore
├── DESIGN.md                 # 已存在
├── README.md                 # Task 13
├── assets/
│   └── tray.png              # 托盘图标(Task 11 放置)
├── src/
│   ├── core/                 # 纯逻辑,可单测,不依赖 Electron
│   │   ├── ads.js            # 预置假广告模板数据
│   │   ├── adRotator.js      # 轮播下一条/随机另一条
│   │   ├── videoScanner.js   # 扫描文件夹→视频列表
│   │   └── config.js         # 默认配置 + 合并
│   ├── main/
│   │   ├── main.js           # 主进程入口:app 生命周期、IPC、热键、托盘
│   │   ├── window.js         # 创建无边框右下角置顶窗口
│   │   ├── tray.js           # 系统托盘
│   │   └── store.js          # 配置文件读写(包 core/config)
│   ├── preload/
│   │   └── preload.js        # contextBridge 暴露 api
│   └── renderer/
│       ├── index.html        # 三段式布局 + 全屏广告 overlay
│       ├── style.css         # 假广告骚气样式
│       └── renderer.js       # 广告渲染/轮播、视频列表/播放、Boss Key、假关闭
└── tests/
    ├── ads.test.js
    ├── adRotator.test.js
    ├── videoScanner.test.js
    └── config.test.js
```

---

### Task 1: 项目脚手架

**Files:**
- Create: `package.json`
- Create: `vitest.config.js`
- Create: `.gitignore`

- [ ] **Step 1: 写 `package.json`**

```json
{
  "name": "paid-to-watch",
  "version": "0.1.0",
  "description": "伪装成右下角广告弹窗的本地视频摸鱼播放器",
  "main": "src/main/main.js",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "vitest": "^2.0.0"
  }
}
```

- [ ] **Step 2: 写 `vitest.config.js`**

```js
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['tests/**/*.test.js'],
  },
});
```

- [ ] **Step 3: 写 `.gitignore`**

```
node_modules/
dist/
*.log
```

- [ ] **Step 4: 安装依赖**

Run: `npm install`
Expected: 生成 `node_modules/` 与 `package-lock.json`,无报错。

- [ ] **Step 5: 验证测试框架可跑(暂无测试)**

Run: `npm test`
Expected: vitest 输出 "No test files found" 或 0 passed,进程正常退出(不报错即可)。

- [ ] **Step 6: Commit**

```bash
git add package.json vitest.config.js .gitignore package-lock.json
git commit -m "chore: 项目脚手架(Electron + vitest)"
```

---

### Task 2: 假广告模板数据(`core/ads.js`)

**Files:**
- Create: `src/core/ads.js`
- Test: `tests/ads.test.js`

- [ ] **Step 1: 写失败测试**

```js
// tests/ads.test.js
import { describe, it, expect } from 'vitest';
import ads from '../src/core/ads.js';

const { AD_TEMPLATES } = ads;

describe('AD_TEMPLATES', () => {
  it('至少有 5 套模板', () => {
    expect(AD_TEMPLATES.length).toBeGreaterThanOrEqual(5);
  });

  it('每套模板都有渲染所需字段', () => {
    for (const ad of AD_TEMPLATES) {
      expect(typeof ad.id).toBe('string');
      expect(typeof ad.title).toBe('string');
      expect(typeof ad.button).toBe('string');
      expect(typeof ad.bg).toBe('string'); // CSS 背景值
    }
  });

  it('id 互不相同', () => {
    const ids = AD_TEMPLATES.map((a) => a.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/ads.test.js`
Expected: FAIL —— 找不到模块 `../src/core/ads.js`。

- [ ] **Step 3: 写实现**

```js
// src/core/ads.js
// 预置假广告模板。越 low 越像牛皮癣 → 伪装越好。
const AD_TEMPLATES = [
  {
    id: 'redpacket',
    title: '🧧 您有一个红包待领取',
    subtitle: '恭喜！系统检测到您可领取 88.88 元',
    button: '立即领取',
    bg: 'linear-gradient(135deg,#ff5f6d,#ffc371)',
  },
  {
    id: 'cleaner',
    title: '⚠️ 检测到 36 个垃圾文件',
    subtitle: '您的电脑运行缓慢，建议立即清理加速',
    button: '一键加速',
    bg: 'linear-gradient(135deg,#36d1dc,#5b86e5)',
  },
  {
    id: 'nearby',
    title: '💃 附近有 3 位用户想认识你',
    subtitle: '距离你 800m，刚刚在线',
    button: '查看详情',
    bg: 'linear-gradient(135deg,#ee9ca7,#ffdde1)',
  },
  {
    id: 'stock',
    title: '📈 这只票今日涨停',
    subtitle: '老股民内部消息，名额仅剩 2 个',
    button: '免费领取',
    bg: 'linear-gradient(135deg,#c31432,#240b36)',
  },
  {
    id: 'sale',
    title: '🛒 全场 9.9 包邮 限时 5 分钟',
    subtitle: '错过再等一年，手慢无',
    button: '马上抢购',
    bg: 'linear-gradient(135deg,#f7971e,#ffd200)',
  },
];

module.exports = { AD_TEMPLATES };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/ads.test.js`
Expected: PASS（3 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/core/ads.js tests/ads.test.js
git commit -m "feat: 预置假广告模板数据"
```

---

### Task 3: 广告轮播逻辑(`core/adRotator.js`)

**Files:**
- Create: `src/core/adRotator.js`
- Test: `tests/adRotator.test.js`

- [ ] **Step 1: 写失败测试**

```js
// tests/adRotator.test.js
import { describe, it, expect } from 'vitest';
import rotator from '../src/core/adRotator.js';

const { nextIndex, randomOtherIndex } = rotator;

describe('nextIndex', () => {
  it('正常自增', () => {
    expect(nextIndex(0, 5)).toBe(1);
  });
  it('到末尾循环回 0', () => {
    expect(nextIndex(4, 5)).toBe(0);
  });
  it('长度为 0 时返回 0', () => {
    expect(nextIndex(0, 0)).toBe(0);
  });
});

describe('randomOtherIndex', () => {
  it('结果落在范围内且不等于当前', () => {
    // 注入可控随机:第一次给 0(==current 被拒),第二次给 0.5 → index 2
    const seq = [0, 0.5];
    let i = 0;
    const rand = () => seq[i++];
    expect(randomOtherIndex(0, 5, rand)).toBe(2);
  });
  it('长度为 1 时返回 0', () => {
    expect(randomOtherIndex(0, 1)).toBe(0);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/adRotator.test.js`
Expected: FAIL —— 找不到模块。

- [ ] **Step 3: 写实现**

```js
// src/core/adRotator.js

// 顺序下一条,循环。
function nextIndex(current, length) {
  if (length <= 0) return 0;
  return (current + 1) % length;
}

// 随机选一条与 current 不同的;length<=1 时只能返回 0。
// rand 可注入以便测试,默认 Math.random。
function randomOtherIndex(current, length, rand = Math.random) {
  if (length <= 1) return 0;
  let i;
  do {
    i = Math.floor(rand() * length);
  } while (i === current);
  return i;
}

module.exports = { nextIndex, randomOtherIndex };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/adRotator.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/core/adRotator.js tests/adRotator.test.js
git commit -m "feat: 假广告轮播逻辑"
```

---

### Task 4: 视频扫描(`core/videoScanner.js`)

**Files:**
- Create: `src/core/videoScanner.js`
- Test: `tests/videoScanner.test.js`

- [ ] **Step 1: 写失败测试**

```js
// tests/videoScanner.test.js
import { describe, it, expect } from 'vitest';
import scanner from '../src/core/videoScanner.js';

const { isVideoFile, filterVideos, scanFolder } = scanner;

describe('isVideoFile', () => {
  it('识别 mp4/webm/ogv/m4v(大小写不敏感)', () => {
    expect(isVideoFile('a.mp4')).toBe(true);
    expect(isVideoFile('B.WEBM')).toBe(true);
    expect(isVideoFile('c.ogv')).toBe(true);
    expect(isVideoFile('d.m4v')).toBe(true);
  });
  it('拒绝非视频与不支持的容器', () => {
    expect(isVideoFile('e.txt')).toBe(false);
    expect(isVideoFile('f.mkv')).toBe(false);
    expect(isVideoFile('noext')).toBe(false);
  });
});

describe('filterVideos', () => {
  it('只留视频文件', () => {
    expect(filterVideos(['a.mp4', 'b.txt', 'c.webm'])).toEqual(['a.mp4', 'c.webm']);
  });
});

describe('scanFolder', () => {
  it('注入 readdir,返回 {name,path}', () => {
    const fakeReaddir = () => ['x.mp4', 'y.jpg', 'z.webm'];
    const result = scanFolder('/videos', fakeReaddir);
    expect(result).toEqual([
      { name: 'x.mp4', path: require('path').join('/videos', 'x.mp4') },
      { name: 'z.webm', path: require('path').join('/videos', 'z.webm') },
    ]);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/videoScanner.test.js`
Expected: FAIL —— 找不到模块。

- [ ] **Step 3: 写实现**

```js
// src/core/videoScanner.js
const fs = require('node:fs');
const path = require('node:path');

const VIDEO_EXTS = new Set(['.mp4', '.webm', '.ogv', '.m4v']);

function isVideoFile(name) {
  return VIDEO_EXTS.has(path.extname(name).toLowerCase());
}

function filterVideos(names) {
  return names.filter(isVideoFile);
}

// readdir 可注入以便测试,默认同步读取目录。
function scanFolder(dir, readdir = fs.readdirSync) {
  return filterVideos(readdir(dir)).map((name) => ({
    name,
    path: path.join(dir, name),
  }));
}

module.exports = { isVideoFile, filterVideos, scanFolder, VIDEO_EXTS };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/videoScanner.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/core/videoScanner.js tests/videoScanner.test.js
git commit -m "feat: 本地视频扫描"
```

---

### Task 5: 配置合并(`core/config.js`)

**Files:**
- Create: `src/core/config.js`
- Test: `tests/config.test.js`

- [ ] **Step 1: 写失败测试**

```js
// tests/config.test.js
import { describe, it, expect } from 'vitest';
import config from '../src/core/config.js';

const { DEFAULT_CONFIG, mergeConfig } = config;

describe('mergeConfig', () => {
  it('空输入返回默认配置副本', () => {
    expect(mergeConfig(null)).toEqual(DEFAULT_CONFIG);
    expect(mergeConfig(undefined)).toEqual(DEFAULT_CONFIG);
    expect(mergeConfig(null)).not.toBe(DEFAULT_CONFIG); // 是副本
  });
  it('用户值覆盖默认值', () => {
    const merged = mergeConfig({ videoFolder: '/v', muted: false });
    expect(merged.videoFolder).toBe('/v');
    expect(merged.muted).toBe(false);
    expect(merged.hotkey).toBe(DEFAULT_CONFIG.hotkey); // 未提供的保留默认
  });
  it('window 子对象做浅合并而非整体替换', () => {
    const merged = mergeConfig({ window: { x: 10, y: 20 } });
    expect(merged.window.x).toBe(10);
    expect(merged.window.width).toBe(DEFAULT_CONFIG.window.width); // 保留默认宽
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/config.test.js`
Expected: FAIL —— 找不到模块。

- [ ] **Step 3: 写实现**

```js
// src/core/config.js
const DEFAULT_CONFIG = {
  videoFolder: null,
  window: { x: null, y: null, width: 360, height: 640 },
  hotkey: 'CommandOrControl+Alt+Z',
  muted: true,
};

function mergeConfig(saved) {
  if (!saved || typeof saved !== 'object') {
    return JSON.parse(JSON.stringify(DEFAULT_CONFIG));
  }
  return {
    ...DEFAULT_CONFIG,
    ...saved,
    window: { ...DEFAULT_CONFIG.window, ...(saved.window || {}) },
  };
}

module.exports = { DEFAULT_CONFIG, mergeConfig };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/config.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/core/config.js tests/config.test.js
git commit -m "feat: 配置默认值与合并"
```

---

### Task 6: 主进程窗口骨架(右下角无边框置顶窗口)

**Files:**
- Create: `src/main/window.js`
- Create: `src/main/main.js`
- Create: `src/preload/preload.js`
- Create: `src/renderer/index.html`(临时最小内容)

- [ ] **Step 1: 写 `src/main/window.js`**

```js
// src/main/window.js
const { BrowserWindow, screen } = require('electron');
const path = require('node:path');

// 依据配置在主屏右下角创建窗口。config.window 提供尺寸/可选坐标。
function createMainWindow(config) {
  const { width, height } = config.window;
  const { width: sw, height: sh } = screen.getPrimaryDisplay().workAreaSize;
  const x = config.window.x ?? sw - width - 20;
  const y = config.window.y ?? sh - height - 20;

  const win = new BrowserWindow({
    width,
    height,
    x,
    y,
    frame: false,
    alwaysOnTop: true,
    resizable: true,
    skipTaskbar: true,
    webPreferences: {
      preload: path.join(__dirname, '../preload/preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
    },
  });
  win.setAlwaysOnTop(true, 'screen-saver');
  win.loadFile(path.join(__dirname, '../renderer/index.html'));
  return win;
}

module.exports = { createMainWindow };
```

- [ ] **Step 2: 写 `src/preload/preload.js`(先占位,后续任务扩展)**

```js
// src/preload/preload.js
const { contextBridge } = require('electron');

contextBridge.exposeInMainWorld('api', {
  ping: () => 'pong',
});
```

- [ ] **Step 3: 写 `src/renderer/index.html`(临时验证窗口)**

```html
<!doctype html>
<html lang="zh">
  <head>
    <meta charset="utf-8" />
    <style>
      body { margin: 0; background: #222; color: #fff; font: 14px sans-serif;
             display: flex; align-items: center; justify-content: center; height: 100vh; }
    </style>
  </head>
  <body>
    窗口骨架 OK
  </body>
</html>
```

- [ ] **Step 4: 写 `src/main/main.js`**

```js
// src/main/main.js
const { app } = require('electron');
const { createMainWindow } = require('./window');
const { DEFAULT_CONFIG } = require('../core/config');

let mainWindow = null;

app.whenReady().then(() => {
  mainWindow = createMainWindow(DEFAULT_CONFIG);
});

app.on('window-all-closed', () => {
  app.quit();
});
```

- [ ] **Step 5: 手动验收**

Run: `npm start`
Expected: 屏幕**右下角**出现一个**无边框、置顶**的小窗(约 360×640),显示「窗口骨架 OK」,任务栏无图标。关闭后进程退出。

- [ ] **Step 6: Commit**

```bash
git add src/main/window.js src/main/main.js src/preload/preload.js src/renderer/index.html
git commit -m "feat: 右下角无边框置顶窗口骨架"
```

---

### Task 7: 三段式 UI + 假广告渲染与轮播

**Files:**
- Modify: `src/renderer/index.html`
- Create: `src/renderer/style.css`
- Create: `src/renderer/renderer.js`

- [ ] **Step 1: 重写 `src/renderer/index.html`**

```html
<!doctype html>
<html lang="zh">
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div id="app">
      <!-- 上横幅假广告 -->
      <div class="ad-banner" id="ad-top"></div>

      <!-- 中间:视频区 -->
      <div class="stage" id="stage">
        <video id="video" muted></video>
        <div class="video-empty" id="video-empty">点击托盘图标选择视频文件夹</div>
        <ul class="playlist" id="playlist"></ul>
      </div>

      <!-- 下横幅假广告 -->
      <div class="ad-banner" id="ad-bottom"></div>

      <!-- 假关闭按钮(拖动手柄也在这条) -->
      <div class="titlebar" id="titlebar">
        <span class="fake-close" id="fake-close">×</span>
      </div>

      <!-- Boss Key:全屏第三条假广告 overlay,默认隐藏 -->
      <div class="boss-overlay" id="boss-overlay" hidden></div>
    </div>
    <script src="renderer.js"></script>
  </body>
</html>
```

- [ ] **Step 2: 写 `src/renderer/style.css`**

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
html, body, #app { height: 100%; width: 100%; }
#app { display: flex; flex-direction: column; background: #000; overflow: hidden;
       font-family: "Microsoft YaHei", sans-serif; }

/* 可拖动标题条 + 假关闭 */
.titlebar { height: 22px; background: #1a1a1a; -webkit-app-region: drag;
            display: flex; justify-content: flex-end; align-items: center; }
.fake-close { -webkit-app-region: no-drag; color: #aaa; font-size: 16px;
              width: 28px; text-align: center; cursor: pointer; }
.fake-close:hover { background: #e81123; color: #fff; }

/* 上下假广告横幅 */
.ad-banner { height: 70px; color: #fff; display: flex; flex-direction: column;
             justify-content: center; padding: 6px 10px; cursor: pointer;
             overflow: hidden; }
.ad-banner .ad-title { font-size: 13px; font-weight: bold; }
.ad-banner .ad-sub { font-size: 11px; opacity: .9; }
.ad-banner .ad-btn { align-self: flex-start; margin-top: 3px; font-size: 11px;
                     background: rgba(255,255,255,.9); color: #c00; border-radius: 10px;
                     padding: 1px 8px; }

/* 中间视频区 */
.stage { flex: 1; position: relative; background: #000; }
#video { width: 100%; height: 100%; object-fit: contain; display: none; }
#video.playing { display: block; }
.video-empty { position: absolute; inset: 0; display: flex; align-items: center;
               justify-content: center; color: #777; font-size: 12px; text-align: center; }
.playlist { position: absolute; left: 0; right: 0; bottom: 0; max-height: 40%;
            overflow-y: auto; background: rgba(0,0,0,.6); list-style: none;
            opacity: 0; transition: opacity .2s; }
.stage:hover .playlist { opacity: 1; }
.playlist li { color: #eee; font-size: 12px; padding: 4px 8px; cursor: pointer;
               white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.playlist li:hover, .playlist li.active { background: rgba(255,255,255,.15); }

/* Boss Key 全屏假广告 */
.boss-overlay { position: fixed; inset: 0; z-index: 9999; color: #fff;
                display: flex; flex-direction: column; align-items: center;
                justify-content: center; text-align: center; padding: 16px; }
.boss-overlay[hidden] { display: none; }
.boss-overlay .b-title { font-size: 18px; font-weight: bold; margin-bottom: 8px; }
.boss-overlay .b-btn { margin-top: 12px; background: #fff; color: #c00;
                       border-radius: 16px; padding: 6px 18px; font-size: 14px; }
```

- [ ] **Step 3: 写 `src/renderer/renderer.js`(本任务只做上下广告渲染 + 轮播)**

```js
// src/renderer/renderer.js
const { AD_TEMPLATES } = window.api.ads();
const { nextIndex } = window.api.rotator;

let topIdx = 0;
let bottomIdx = Math.floor(AD_TEMPLATES.length / 2);

function renderBanner(el, ad) {
  el.style.background = ad.bg;
  el.innerHTML =
    `<div class="ad-title">${ad.title}</div>` +
    `<div class="ad-sub">${ad.subtitle}</div>` +
    `<div class="ad-btn">${ad.button}</div>`;
}

function paintAds() {
  renderBanner(document.getElementById('ad-top'), AD_TEMPLATES[topIdx]);
  renderBanner(document.getElementById('ad-bottom'), AD_TEMPLATES[bottomIdx]);
}

function rotateAds() {
  topIdx = nextIndex(topIdx, AD_TEMPLATES.length);
  bottomIdx = nextIndex(bottomIdx, AD_TEMPLATES.length);
  paintAds();
}

paintAds();
setInterval(rotateAds, 6000); // 每 6s 轮播一次,模拟真广告刷新
```

- [ ] **Step 4: 扩展 `src/preload/preload.js` 暴露 core 给渲染层**

```js
// src/preload/preload.js
const { contextBridge } = require('electron');
const { AD_TEMPLATES } = require('../core/ads');
const { nextIndex, randomOtherIndex } = require('../core/adRotator');

contextBridge.exposeInMainWorld('api', {
  ads: () => ({ AD_TEMPLATES }),
  rotator: { nextIndex, randomOtherIndex },
});
```

- [ ] **Step 5: 手动验收**

Run: `npm start`
Expected: 窗口呈**上中下三段**——上下是彩色假广告横幅(红包/清理/相亲等),中间黑色视频区显示「点击托盘图标选择视频文件夹」。等约 6 秒,上下广告**自动换一条**。顶部有可拖动条和一个「×」。

- [ ] **Step 6: Commit**

```bash
git add src/renderer/ src/preload/preload.js
git commit -m "feat: 三段式 UI 与假广告轮播"
```

---

### Task 8: 选文件夹 + 视频列表 + 播放

**Files:**
- Modify: `src/main/main.js`
- Modify: `src/preload/preload.js`
- Modify: `src/renderer/renderer.js`

- [ ] **Step 1: 主进程加 IPC(选文件夹 / 扫描视频)**

在 `src/main/main.js` 顶部 require 补充,并在 `app.whenReady` 之前注册 handler:

```js
// src/main/main.js
const { app, ipcMain, dialog } = require('electron');
const { pathToFileURL } = require('node:url');
const { createMainWindow } = require('./window');
const { DEFAULT_CONFIG } = require('../core/config');
const { scanFolder } = require('../core/videoScanner');

let mainWindow = null;

ipcMain.handle('pick-folder', async () => {
  const r = await dialog.showOpenDialog(mainWindow, { properties: ['openDirectory'] });
  return r.canceled ? null : r.filePaths[0];
});

ipcMain.handle('scan-videos', (_e, dir) => {
  if (!dir) return [];
  return scanFolder(dir).map((v) => ({ ...v, url: pathToFileURL(v.path).href }));
});

app.whenReady().then(() => {
  mainWindow = createMainWindow(DEFAULT_CONFIG);
});

app.on('window-all-closed', () => app.quit());
```

- [ ] **Step 2: preload 暴露选择/扫描接口**

在 `src/preload/preload.js` 的 `exposeInMainWorld` 对象里追加:

```js
const { ipcRenderer } = require('electron');
// ...在已有 contextBridge.exposeInMainWorld('api', { ... }) 中加入:
//   pickFolder: () => ipcRenderer.invoke('pick-folder'),
//   scanVideos: (dir) => ipcRenderer.invoke('scan-videos', dir),
```

完整文件:

```js
// src/preload/preload.js
const { contextBridge, ipcRenderer } = require('electron');
const { AD_TEMPLATES } = require('../core/ads');
const { nextIndex, randomOtherIndex } = require('../core/adRotator');

contextBridge.exposeInMainWorld('api', {
  ads: () => ({ AD_TEMPLATES }),
  rotator: { nextIndex, randomOtherIndex },
  pickFolder: () => ipcRenderer.invoke('pick-folder'),
  scanVideos: (dir) => ipcRenderer.invoke('scan-videos', dir),
});
```

- [ ] **Step 3: 渲染层加视频列表与播放(临时用按钮触发选择,下个任务移到托盘)**

在 `src/renderer/renderer.js` 末尾追加:

```js
const videoEl = document.getElementById('video');
const emptyEl = document.getElementById('video-empty');
const playlistEl = document.getElementById('playlist');

async function loadFolder() {
  const dir = await window.api.pickFolder();
  if (!dir) return;
  const videos = await window.api.scanVideos(dir);
  renderPlaylist(videos);
  if (videos.length) play(videos[0], 0);
}

function renderPlaylist(videos) {
  playlistEl.innerHTML = '';
  videos.forEach((v, i) => {
    const li = document.createElement('li');
    li.textContent = v.name;
    li.onclick = () => play(v, i);
    playlistEl.appendChild(li);
  });
  emptyEl.style.display = videos.length ? 'none' : 'flex';
}

function play(video, index) {
  videoEl.src = video.url;
  videoEl.classList.add('playing');
  videoEl.play();
  [...playlistEl.children].forEach((li, i) =>
    li.classList.toggle('active', i === index)
  );
}

// 临时:双击视频区触发选择文件夹(Task 10 改由托盘菜单触发)
document.getElementById('stage').addEventListener('dblclick', loadFolder);

// 暴露给托盘任务复用
window.__loadFolder = loadFolder;
```

- [ ] **Step 4: 手动验收**

Run: `npm start`
1. **双击中间视频区** → 弹出系统选择文件夹对话框。
2. 选一个含 `.mp4`/`.webm` 视频的文件夹 → 自动播放第一个,鼠标移到视频区底部出现**播放列表**,点选切换。
3. 视频默认**静音**播放。

- [ ] **Step 5: Commit**

```bash
git add src/main/main.js src/preload/preload.js src/renderer/renderer.js
git commit -m "feat: 选择文件夹、视频列表与播放"
```

---

### Task 9: Boss Key(全局热键 → 视频秒变全屏广告)

**Files:**
- Modify: `src/main/main.js`
- Modify: `src/preload/preload.js`
- Modify: `src/renderer/renderer.js`

- [ ] **Step 1: 主进程注册全局热键并广播**

在 `src/main/main.js`:`require` 增加 `globalShortcut`;在 `app.whenReady().then` 内创建窗口后注册热键;并在退出时注销。

```js
// 顶部:const { app, ipcMain, dialog, globalShortcut } = require('electron');

app.whenReady().then(() => {
  mainWindow = createMainWindow(DEFAULT_CONFIG);

  globalShortcut.register(DEFAULT_CONFIG.hotkey, () => {
    mainWindow.webContents.send('toggle-boss-key');
  });
});

app.on('will-quit', () => globalShortcut.unregisterAll());
```

- [ ] **Step 2: preload 暴露 Boss Key 订阅**

在 `src/preload/preload.js` 的 api 对象追加:

```js
  onBossKey: (cb) => ipcRenderer.on('toggle-boss-key', () => cb()),
```

- [ ] **Step 3: 渲染层切换全屏广告 overlay**

在 `src/renderer/renderer.js` 末尾追加:

```js
const { randomOtherIndex } = window.api.rotator;
const overlay = document.getElementById('boss-overlay');
let bossOn = false;

function renderBossAd() {
  const ad = AD_TEMPLATES[randomOtherIndex(topIdx, AD_TEMPLATES.length)];
  overlay.style.background = ad.bg;
  overlay.innerHTML =
    `<div class="b-title">${ad.title}</div>` +
    `<div class="ad-sub">${ad.subtitle}</div>` +
    `<div class="b-btn">${ad.button}</div>`;
}

window.api.onBossKey(() => {
  bossOn = !bossOn;
  if (bossOn) {
    renderBossAd();
    overlay.hidden = false;
    videoEl.pause();
  } else {
    overlay.hidden = true;
    videoEl.play();
  }
});
```

- [ ] **Step 4: 手动验收**

Run: `npm start`,先选好文件夹播上视频。
1. 按 **Ctrl+Alt+Z** → 中间视频**瞬间被一张全屏假广告盖住**,视频暂停。
2. 再按一次 → 广告消失,视频从原位置继续播。
3. 在别的窗口获得焦点时按热键**依然生效**(全局热键)。

- [ ] **Step 5: Commit**

```bash
git add src/main/main.js src/preload/preload.js src/renderer/renderer.js
git commit -m "feat: Boss Key 一键全屏假广告"
```

---

### Task 10: 假关闭按钮(点了假装关掉又弹回)

**Files:**
- Modify: `src/main/main.js`
- Modify: `src/preload/preload.js`
- Modify: `src/renderer/renderer.js`

- [ ] **Step 1: 主进程加「假关闭」IPC**

在 `src/main/main.js` 注册:

```js
ipcMain.on('fake-close', () => {
  if (!mainWindow) return;
  mainWindow.hide();
  setTimeout(() => mainWindow.show(), 150); // 假装关掉,150ms 后又弹回
});
```

- [ ] **Step 2: preload 暴露**

在 api 对象追加:

```js
  fakeClose: () => ipcRenderer.send('fake-close'),
```

- [ ] **Step 3: 渲染层绑定假关闭按钮**

在 `src/renderer/renderer.js` 末尾追加:

```js
document.getElementById('fake-close').addEventListener('click', () => {
  window.api.fakeClose();
});
```

- [ ] **Step 4: 手动验收**

Run: `npm start`
点右上角「×」→ 窗口**一闪而隐、随即又弹回**右下角(复刻流氓广告关不掉的感觉)。

- [ ] **Step 5: Commit**

```bash
git add src/main/main.js src/preload/preload.js src/renderer/renderer.js
git commit -m "feat: 假关闭按钮(关不掉的梗)"
```

---

### Task 11: 系统托盘(选文件夹 / 显隐 / 退出)

**Files:**
- Create: `src/main/tray.js`
- Create: `assets/tray.png`
- Modify: `src/main/main.js`

- [ ] **Step 1: 放置托盘图标**

放一张 **32×32 PNG** 到 `assets/tray.png`(临时随便一张纯色或表情图标即可,正式图标后续替换)。

- [ ] **Step 2: 写 `src/main/tray.js`**

```js
// src/main/tray.js
const { Tray, Menu, app } = require('electron');
const path = require('node:path');

// callbacks: { onPickFolder, onToggleShow }
function createTray(win, callbacks) {
  const tray = new Tray(path.join(__dirname, '../../assets/tray.png'));
  const menu = Menu.buildFromTemplate([
    { label: '选择视频文件夹', click: () => callbacks.onPickFolder() },
    { label: '显示 / 隐藏', click: () => callbacks.onToggleShow() },
    { type: 'separator' },
    { label: '退出', click: () => app.quit() },
  ]);
  tray.setToolTip('带薪看片');
  tray.setContextMenu(menu);
  return tray;
}

module.exports = { createTray };
```

- [ ] **Step 3: 主进程接线托盘**

在 `src/main/main.js`:require `createTray`;在创建窗口后建托盘。「选择视频文件夹」通过给渲染层发消息复用已有 `__loadFolder`:

```js
// 顶部:const { createTray } = require('./tray');
// 渲染层订阅(见 Step 4)后,主进程这样触发:

let tray = null;
app.whenReady().then(() => {
  mainWindow = createMainWindow(DEFAULT_CONFIG);
  globalShortcut.register(DEFAULT_CONFIG.hotkey, () => {
    mainWindow.webContents.send('toggle-boss-key');
  });
  tray = createTray(mainWindow, {
    onPickFolder: () => mainWindow.webContents.send('pick-folder-from-tray'),
    onToggleShow: () => (mainWindow.isVisible() ? mainWindow.hide() : mainWindow.show()),
  });
});
```

- [ ] **Step 4: 渲染层响应托盘「选择文件夹」**

preload 的 api 追加:

```js
  onPickFolderFromTray: (cb) => ipcRenderer.on('pick-folder-from-tray', () => cb()),
```

`src/renderer/renderer.js` 末尾追加:

```js
window.api.onPickFolderFromTray(() => window.__loadFolder());
```

- [ ] **Step 5: 手动验收**

Run: `npm start`
1. 系统托盘出现图标,悬停提示「带薪看片」。
2. 右键 → **选择视频文件夹** → 弹出选择框,选后播放。
3. **显示 / 隐藏** 能切换窗口可见性。
4. **退出** 真正结束进程。

- [ ] **Step 6: Commit**

```bash
git add src/main/tray.js src/main/main.js src/preload/preload.js src/renderer/renderer.js assets/tray.png
git commit -m "feat: 系统托盘菜单"
```

---

### Task 12: 配置持久化(记住文件夹、窗口位置与大小)

**Files:**
- Create: `src/main/store.js`
- Modify: `src/main/main.js`
- Modify: `src/preload/preload.js`
- Modify: `src/renderer/renderer.js`

- [ ] **Step 1: 写 `src/main/store.js`**

```js
// src/main/store.js
const fs = require('node:fs');
const path = require('node:path');
const { app } = require('electron');
const { mergeConfig } = require('../core/config');

const file = () => path.join(app.getPath('userData'), 'config.json');

function loadConfig() {
  try {
    return mergeConfig(JSON.parse(fs.readFileSync(file(), 'utf-8')));
  } catch {
    return mergeConfig(null); // 文件不存在或损坏 → 默认配置
  }
}

function saveConfig(cfg) {
  fs.writeFileSync(file(), JSON.stringify(cfg, null, 2), 'utf-8');
}

module.exports = { loadConfig, saveConfig };
```

- [ ] **Step 2: 主进程用持久化配置,并保存文件夹与窗口位置**

`src/main/main.js` 改动:用 `loadConfig()` 取代 `DEFAULT_CONFIG`;记录 videoFolder;监听窗口移动/缩放保存 bounds。

```js
// 顶部:const { loadConfig, saveConfig } = require('./store');

let config = loadConfig();

ipcMain.handle('get-config', () => config);
ipcMain.handle('set-folder', (_e, dir) => {
  config.videoFolder = dir;
  saveConfig(config);
});

app.whenReady().then(() => {
  mainWindow = createMainWindow(config); // 用持久化的 window 尺寸/坐标

  const persistBounds = () => {
    const b = mainWindow.getBounds();
    config.window = { ...config.window, x: b.x, y: b.y, width: b.width, height: b.height };
    saveConfig(config);
  };
  mainWindow.on('moved', persistBounds);
  mainWindow.on('resized', persistBounds);

  globalShortcut.register(config.hotkey, () => {
    mainWindow.webContents.send('toggle-boss-key');
  });
  tray = createTray(mainWindow, {
    onPickFolder: () => mainWindow.webContents.send('pick-folder-from-tray'),
    onToggleShow: () => (mainWindow.isVisible() ? mainWindow.hide() : mainWindow.show()),
  });
});
```

- [ ] **Step 3: preload 暴露 config 接口**

api 追加:

```js
  getConfig: () => ipcRenderer.invoke('get-config'),
  setFolder: (dir) => ipcRenderer.invoke('set-folder', dir),
```

- [ ] **Step 4: 渲染层启动时自动载入上次文件夹,选择后记住**

改 `src/renderer/renderer.js` 的 `loadFolder`,并在启动时自动恢复:

```js
async function loadFolder() {
  const dir = await window.api.pickFolder();
  if (!dir) return;
  await window.api.setFolder(dir);
  await openFolder(dir);
}

async function openFolder(dir) {
  const videos = await window.api.scanVideos(dir);
  renderPlaylist(videos);
  if (videos.length) play(videos[0], 0);
}

// 启动时:有记住的文件夹就直接加载
(async () => {
  const cfg = await window.api.getConfig();
  if (cfg.videoFolder) await openFolder(cfg.videoFolder);
})();
```

> 注意:删除 Task 8 Step 3 里旧的 `loadFolder` 定义,用本任务的版本替换;`window.__loadFolder = loadFolder;` 保留。

- [ ] **Step 5: 手动验收**

Run: `npm start`
1. 选一个文件夹播放 → **关掉 app 再 `npm start`** → 自动恢复上次文件夹并播放。
2. **拖动窗口、调大小** → 重启后位置与尺寸保持。

- [ ] **Step 6: Commit**

```bash
git add src/main/store.js src/main/main.js src/preload/preload.js src/renderer/renderer.js
git commit -m "feat: 配置持久化(文件夹/窗口位置与大小)"
```

---

### Task 13: README 与打包

**Files:**
- Create: `README.md`
- Modify: `package.json`

- [ ] **Step 1: 写 `README.md`(自黑梗 + 用法 + 免责)**

````markdown
# 带薪看片 (paid-to-watch)

> 史上最敬业的摸鱼工具——把视频藏进一个「假广告弹窗」里。老板路过,只当你又中了流氓广告。

![demo](docs/demo.gif)

## 它是什么

一个常驻桌面右下角、长得像垃圾广告弹窗的本地视频播放器。上下两条是**假广告**(伪装色),中间偷偷播你自己的视频。一个热键就能把视频秒变全屏广告。

## 用法

```bash
npm install
npm start
```

- 右键**托盘图标** → 选择视频文件夹(支持 `.mp4 .webm .ogv .m4v`)
- 鼠标移到视频区底部 → 播放列表
- **Boss Key:`Ctrl+Alt+Z`** → 视频 ↔ 全屏假广告 一键切换
- 那个「×」是假的,点了它会「关不掉」😈

## 免责声明

本项目纯属娱乐,用于演示「注意力盲区」这个有趣的交互点子。请遵守你所在单位的规章制度,摸鱼后果自负。它**不保证**不被发现。

## License

MIT
````

- [ ] **Step 2: 加入打包配置(electron-builder)**

`package.json` 增加 devDependency 与 scripts、build 配置:

```json
{
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:watch": "vitest",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.0.0",
    "vitest": "^2.0.0"
  },
  "build": {
    "appId": "com.stevenliang.paidtowatch",
    "productName": "带薪看片",
    "files": ["src/**/*", "assets/**/*"],
    "win": { "target": "portable" }
  }
}
```

- [ ] **Step 3: 安装并打包**

Run: `npm install && npm run dist`
Expected: `dist/` 下生成可直接双击运行的 Windows portable exe。

- [ ] **Step 4: 手动验收**

双击 `dist/` 里的 exe(在干净路径下)→ 不依赖 node/npm 即可启动,功能与 `npm start` 一致。

- [ ] **Step 5: Commit**

```bash
git add README.md package.json package-lock.json
git commit -m "docs: README 与打包配置"
```

---

## 录制 demo.gif(发布前,非代码任务)

Boss Key 那一下是传播引爆点。发布前录一段 5 秒 GIF:正常播视频 → 有人「走近」→ 按 Ctrl+Alt+Z → 秒变广告。放到 `docs/demo.gif`,README 已引用。

---

## 自检结果(writing-plans 第 7 步,作者已跑)

- **Spec 覆盖**:DESIGN 的窗口/假广告/视频/Boss Key/假关闭/托盘/持久化/传播包装/格式边界,逐项都有对应 Task(见上)。声音=默认静音 → Task 6 video `muted` + Task 8 验收点。
- **占位符**:无 TBD/TODO;集成任务以「手动验收」替代自动化测试(已在计划开头声明的测试策略)。
- **类型/命名一致**:`AD_TEMPLATES / nextIndex / randomOtherIndex / scanFolder / mergeConfig / createMainWindow / loadConfig / saveConfig` 在定义与调用处一致;IPC 频道名(`pick-folder / scan-videos / toggle-boss-key / fake-close / pick-folder-from-tray / get-config / set-folder`)主进程注册与 preload 调用一致。
- **已知衔接点**:Task 12 Step 4 明确要求替换 Task 8 的旧 `loadFolder`,避免重复定义。
```